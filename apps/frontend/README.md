# Frontend

Reserved for the future Product Plane UI. Phase 5 does not implement a
frontend. Future UI code must consume framework-neutral BYQ contracts through
the Gateway and must not depend directly on DSH internal event schemas.
