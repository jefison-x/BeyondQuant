# H4 学习记录恢复

2026-09-13，`.70`，遵循Accepted ADR-0010，保持人工审核与有限迭代边界。

已复现最后一轮produced写入后进入awaiting_review，重启Store原键重放被错误拒绝。
现在在锁定原run并校验owner/actor后先读原回执，再决定是否准许新迭代；审核也锁定同一行。
run/signal/lesson既有唯一键使用有界同键锁，不添加新账本或自动调度。
原键只读入口复用既有数据，MCP各自读取工具增加封闭参数分支，校验对象和父任务。

Backend学习恢复、循环、HTTP共9项通过；追加HTTP可信工作区检查后恢复/API4项通过。
三个独立Store和数据库连接同键创建、并发最后一轮只产生同一个run/iteration；
原键按四类对象读取，foreign owner拒绝，缺失原键不冒充确认。MCP编译与学习翻译/错对象/
错父任务/未知提交读取参数验证通过。未调用真实模型，无UI改动。

剩余：真实服务进程故障旅程、完整接口证据映射与H5模型研究仍需独立验收。
