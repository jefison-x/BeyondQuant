# H4 回测精确对象读取与未知回执指引

2026-09-13，`.77`。失败测试证明MCP把请求A但收到B的回测响应当成功，不表示生产已发生串结果。

已知ID的回测摘要/分析/运行/取消、派生回测任务读取/执行/取消、信号快照读取须匹配原ID；
信号须为signal_snapshot，分析须为当前公开schema。写请求收到错对象保持outcome_unknown，
读请求报invalid_response，不使用返回的替代对象推进研究。
回测及派生任务创建丢回执时返回精确原task/key只读核对工具；运行/取消按原对象ID核对。
没有自动重试写入、额外执行授权或轮询循环。

MCP编译、原Backtest完整翻译测试及新增错对象/未知结果指引测试通过；
新测试纳入npm test。原分析夹具补真实响应已有的顶层job_id，未放宽产品校验。

`.73`远端运行34737828603最终Backend、integration及其余组件通过，frontend旧反馈夹具失败，
最终门禁失败。`.76`已修复该夹具，本批连同`.74`–`.76`提交新的CI，不沿用旧绿色结果。
H4逐接口台账、更多跨服务故障与H5完整模型研究仍待验收。
