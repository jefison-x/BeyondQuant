# H4 数据需求提交与恢复

2026-09-13，构建 `.67`。H5历史指数准备的既有数据需求入口审计发现：修复任务先提交，
用户需求后提交。真实 PostgreSQL 故障测试确认需求保存失败仍遗留1个修复任务。

现在按workspace和原键加事务锁，用户需求及全部修复引用同事务提交；Worker只能看到提交后的任务。
同键重试先取冻结计划，不因security master或行情更新重新规划/重复准备；不同输入仍冲突。
原键 GET 按owner/workspace精确核对，MCP未知回执提供该只读入口，不通过列表推断不存在。
按ID读错对象和成功写入缺少有效identity不再冒充成功。无新表、队列或Provider权限。

验证：数据需求、真实并发/重启/回滚、数据同步和指数缓存33项通过；MCP编译与翻译测试通过。
并发三请求只有一个需求、一个修复计划；重开Store后不重新规划，异主不能回读，改变日期冲突。
故障修复前测试失败（1个孤立任务），修复后通过（0个）。

这是既有stock-pool数据需求恢复，不代表历史指数准备或完整H4/F7已完成。
