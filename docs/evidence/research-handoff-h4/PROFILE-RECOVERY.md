# H4 模型档案原配置恢复

2026-09-13，候选 `.80`。

模型档案以 owner/key_name 唯一约束避免同键重复对象，但原创建应答丢失后没有精确核对入口。
新增 Product API → Backend 只读原键核对，返回不可变的原配置、原 ID 和创建版本 1；
删除只改变状态/版本，不能使原创建事实消失。该入口不返回密钥，也不扩大模型或 Agent 能力。

页面写前按用户/工作区保存原配置，未知结果刷新后核对。确认必须匹配完整原配置，不能凭同名
认领其他配置；原内容重试仍使用同一唯一键，冲突不改键重建。先前未知请求不因后续拒绝清除。

- 新入口测试修复前 404 失败；修复后凭据/档案 Backend 定向 12 项通过，覆盖删除后原创建和跨用户拒绝。
- 前端提交记录、凭据及 settings 3 文件 10 项通过，覆盖同键不同配置、错 ID 和持久存储失败。
- 真实隔离 Gateway/Product API 浏览器：凭据与档案两项通过，commit 后丢弃应答，刷新 GET 找回
  原对象和配置；各自创建 POST 只有一次。合成凭据不调用 Provider。
- 前端构建通过；最终构建身份、架构及远端 CI 另按最终候选核对。

H4 全接口/有界纠错及 H5 完整研究验收仍未完成。

`.79` 远端 CI 在 secret scan 阶段失败，组件未运行。相同固定扫描器定位两条显式 synthetic
测试值（浏览器不落密钥与内存测试 cipher 轮换），均非 Provider 凭据。仅增加两个历史 commit/file/rule/line
精确指纹排除，保留通用密钥扫描；复扫通过。`.78` 后端未结束即因后续 push 被取消，不记全量通过。
