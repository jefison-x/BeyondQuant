# H4/H5 历史指数修复的缓存复用

2026-09-13。现有 sync_declared_inputs 每次对全部 index_weight_periods 请求 Provider，
即使已存在已验证缓存。先以合成 Provider 和真实 PostgreSQL 复现重复取数，再修复。

现在只有来源为 Tushare index_weight、取数时间明确晚于月末、行数与快照集合完整、
权重/成员数量/内容摘要及来源一致的闭合月份可复用。读取使用只读重复读事务，防止混合刷新快照；
月行数上限50000。缺失或尚未闭合月份继续现有修复；损坏/来源不明证据抛错停止，不重新下载覆盖。
没有扩展 Provider、调度器、队列、数据集目录或生产权限。

本地40项数据同步/readiness/缓存回归通过。缓存专项覆盖缺月才取数、重开Store不重复取数、
五类证据损坏阻止Provider、月内取数不能假作完整月。修改前缺月测试因同时请求已缓存月份失败。
这是 BYQ 缓存复用，不是 Community 缓存迁移；没有生产数据下载或写入。

只读生产覆盖核查使用显式 READ ONLY 事务，不初始化 Store：沪深300已有48份已验证快照，
范围20211231–20260831；202309–202608区间缺202503、202509两个月。
其他五个支持指数各仅3份近期快照（20260630–20260831）。日期范围和数量不代表历史研究readiness全部通过。
H5仍需历史准备入口、必要缺失数据、完整研究与真实模型验收，不能以本测试的合成数据代替。
