# H4 网页证据成功回执与原键恢复

2026-09-13，最终候选 `.87`。MCP 失败测试复现：HTTP 201 空对象被投影为“研究记录已保存”。
现在必须核对原请求派生键、原目标、合法任务与 Artifact ID、父子关系、证据类型及来源数量。
无效成功回执保持 outcome_unknown 和原键，不显示保存成功，不自动重写。
未知结果指向既有 byq_research_get：先按原键读取任务，再以已确认任务 ID 和派生原键读取 Artifact。
未增加工具、后台循环或业务授权。

- MCP 三个定向程序通过，包括10个错误回执反例及原键恢复指引。
- 24个无需完整服务栈的 MCP 测试程序通过；完整 npm test 在 HTTP 合同入口因未配置测试 MCP Token 停止，不能计作完整通过。完整服务合同留远端 CI。
- Backend 网页证据 API 6项通过：真实 PostgreSQL 提交、替换存储实例后仅原键 GET 恢复任务和 Artifact、外部 owner 不可见；禁止创建和列表回退。
- 初次测试夹具误带系统生成 source_id 导致422，按现行创建合同修正后通过。
- `.85` 保留首次候选清单；`.86` 为夹具修订候选，最终对齐原键去除首尾空白规则后使用 `.87`，不覆盖历史身份。
- 124项架构检查通过；无前端改动，无真实模型调用或生产部署。

本切片不关闭该工具全部资格：网页记录并发原键提交、存储等待界限及 F7 持久纠错仍需核对。
全接口台账缺行仍明确未完成，H5真实模型研究及历史覆盖缺口保留。
