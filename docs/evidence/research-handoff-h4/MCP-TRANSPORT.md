# H4 MCP HTTP 入口异常隔离与真实回执合同

2026-09-13，最终候选 `.90`。

真实MCP进程反例：Host为`[`的请求导致未捕获URL异常，进程退出码1。
路由现使用固定本地URL基址；畸形目标返回400。异步handler异常安全503/断连，不使全部会话失去MCP，
也不把可能已提交的写入当成未执行，不自动重放。

另一反例确认，超过观察上限的请求会继续进入后续处理器。现拒绝不可解析/超限的POST，
请求体接收5秒，普通512KiB、既有因子4MiB；成功解析交给SDK复用，失败取消流两分支。
SDK仍负责正常工具schema验证；不以HTTP接收界限限制Agent总时长或正常SSE。

`.87` CI run34744637014的MCP和集成失败保留。根因是新增MCP校验错误要求通用对象公开幂等键，
但Backend现有投影明确移除该字段。不是旧测试错误。修复在网页保存响应单独提供原record键，
MCP验证该键以及目标/对象/父子/来源数量；通用任务和Artifact投影保持不公开内部键。

最终验证：完整npm test的26个程序通过，包含实际MCP→当前Backend→PostgreSQL合同。
实际保存、同原键重复与原Artifact ID回读均通过；此前只跑24个无栈测试不能覆盖这一合同问题。
畸形Host/URL后进程继续存活；超限/坏JSON/坏UTF8/持续不结束的请求不执行工具；
1MiB因子封套仍可进入后续schema处理，4MiB以上拒绝，慢流约5秒取消双方分支。
专用Backend容器和MCP进程测试后已清理；无模型密钥、付费调用或生产操作。

人工HTTP入口面单独核对完成，不把81个工具的领域恢复/F7资格一起标为完成。
普通接口台账87/556，人工入口剩余8类。研究GET引用的research.py新增网页返回字段后已核对更新摘要。

Backend网页API/原键恢复/八连接及原子回滚8项通过；124项架构检查和提交前源码检查通过。
