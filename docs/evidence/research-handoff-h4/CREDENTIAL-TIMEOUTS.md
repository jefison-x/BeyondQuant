# H4 凭据短事务等待上限及逐接口台账

2026-09-13，候选 `.81`。

独立连接持锁复现凭据更新、撤销、模型档案删除超过 3 秒仍等待；释放锁后旧请求继续提交。
凭据存储现在将进程内锁等待限制为 2 秒、数据库锁等待 2 秒、单语句 5 秒，失败退出并回滚，
使用安全的存储不可用错误。私有模型解析也返回安全 503，不误报模型选择失效或输出数据库诊断。
这里限定短元数据操作，不涉及长研究总时限、模型请求或 Worker 任务时长。

- 三个数据库竞争失败测试修复前全部失败；修复后凭据/档案/绑定并发定向 17 项通过。
- 独立补充进程锁等待和私有解析错误两项通过；被拒写入在锁释放后仍保持原版本，无迟到提交。
- 没有 UI 改动；沿用 `.80` 具名页面证据，不声称本切片执行新浏览器流程。

逐接口台账现有 60 行，56 行具名验证，4 行保留未决：档案删除与模型绑定的 Backend/Product
原提交恢复/绑定审计。源枚举 552 项，另 492 项尚未写入最终台账，不表示全有缺陷。
`check-reliability-review.py` 校验源摘要、唯一身份及证据文件，并单独列出九类手工 surface。
当前输出 errors=[]、complete=false；严格完成模式必须拒绝。台账工具不能替代接口审计。
H4/H5 继续，未合并、未部署，也未进行付费模型调用。

最终 `.82` 仅将依赖文件摘要标记为明确的 sha256 值，并同步台账校验器，避免文件名 credentials.py
旁的裸摘要被误认作 API key。`.81` 已生成清单保留；未推送的提交在扫描通过前整理，不排除该文件的扫描。
