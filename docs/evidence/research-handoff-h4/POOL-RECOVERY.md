# H4 股票池创建原键恢复

2026-09-13，`.69`，维护切片，Product Phase 不变。

修复前以三个独立 Store/数据库连接并发提交同一自建池创建键，产生三个不同 pool_id；失败用例先于修复执行。
现在既有 write receipt 与股票池、快照同事务提交，同键锁限制等待2秒，冲突输入拒绝，原键重放返回原对象。
HTTP 创建强制原键及可信工作区，跨工作区重放拒绝；内部无回执夹具仍可由可信 Store 调用。
动态池创建补同范围锁；停用定义返回 run=null，不冒充物化成功。

新增 Product 原键核对，按用户、工作区、类型查询既有回执；MCP 复用 byq_pool_get 的封闭原键分支，
不扩大创建权限。拒绝错 pool_id、错 run.pool_id、损坏回执，创建未知结果提供明确只读核对参数。
三种股票池表单在网络写之前持久保存原键和冻结输入；刷新可核对或人工原键重试，未决输入不被新输入覆盖。
日期默认值在首次提交前固定，切换工作区不会清除其他工作区的提交记录。

## 验证

- Backend 股票池/动态池/信号/回测引用定向回归25项通过；随后追加跨工作区重放检查，最终同组25项再次通过。
- 前端池 API、提交持久记录、视图15项通过；类型和构建通过。
- MCP 编译及股票池翻译通过，补错对象、错任务绑定、损坏与不存在回执反例通过。
- 实际 Chromium→静态前端→Gateway/Product API→Backend→隔离PostgreSQL：真实创建提交后丢弃浏览器
  回执，刷新并只读核对原键找到同一pool_id，全程仅一次POST，1项通过（2.6秒）。非mock后端。
- 浏览器测试保留在 h4-pool-recovery.spec.ts，必须显式启用隔离验证环境。

这批仅关闭创建恢复缺口，股票池其他写操作仍需独立映射；不是H4全接口或H5完整研究完成证明。

`.69`不可变构建清单、开发检查及124项架构检查通过；远端CI对本次新提交另行执行。
