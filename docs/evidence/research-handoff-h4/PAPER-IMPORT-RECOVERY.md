# H4 账户导入恢复

2026-09-13，`.76`。补齐 ADR-0021 的导入幂等要求。

导入账户、导入流水、transfer审计和原键回执在同一事务中提交，原键及目标名称锁等待最多2秒。
Product页面先保存原文件内容和键；回执丢失可刷新后按原键只读核对。
服务资产导入未显式给键时，以完整已校验bundle摘要派生稳定键；摘要验证仍先于重放。
原键换workspace或内容仍冲突，不借重放覆盖账户或导入所有权。
页面的持久记录与文件有4 MiB上限，浏览器无法保存时在发出请求前报错；不允许丢失原输入后静默新建。

- 新接口合同测试修复前因缺少原键参数失败；未将此计为生产事故。
- Backend导入/命令回执/交易/API定向20项通过；追加同键双连接并发、workspace冲突、服务摘要键，导入3项通过。
- 前端13项通过，类型构建和MCP构建/纸面读取测试通过。
- 真实Product浏览器2项通过（5.3秒）：原创建→下单丢回执链仍通过，导入成功后丢回执→刷新只读取回同一account_id，导入POST仅1次。
- 同批修正`.73`远端旧反馈mock用户缺workspace字段，保留显式预览/确认及管理员详情断言；相关2项浏览器通过。
  该mock修正不代替此前真实反馈页面证据，`.73`失败历史保留。

全部操作限隔离测试栈。全接口恢复台账及H5模型研究继续，未执行生产部署。
