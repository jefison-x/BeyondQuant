# H4 研究存储等待与网页记录并发

2026-09-13，`.88`。

实际 PostgreSQL 行锁反例证明，研究状态写入超过3秒仍排队，解锁后才继续提交。
ResearchStore 的短事务现限制进程锁2秒、数据库锁2秒、单条SQL5秒，异常回滚并映射安全503。
同进程锁阻塞的GET也有界失败。该规则不限制长研究、Agent回合或Python领域计算总时长；
也不声称为连接建立、网络、外部调用或整条请求提供统一总期限。
原显式回执查询1秒SQL界限仍可在事务内收紧；handoff只读一致性快照与续接预算不变。

网页记录八个独立连接同原键提交曾触发唯一约束冲突。现先锁原任务派生键，再锁原Artifact派生键，
与既有通用研究创建采用相同锁域；规范化任务/证据仍一次事务提交，未增加自动写重试。
中途Artifact插入故障回滚新任务，无孤立任务或半条证据。

验证：

- 研究API/状态转换/原键核对/回执watch/持久交接/续接及首个行锁反例共82项通过。
- 进程锁、SQL超时、行锁、网页八连接并发及网页API恢复10项通过。
- 最终补充原子故障回滚和八连接测试2项通过；以上集合存在重叠，不累加为独立测试数。
- 锁失败后任务仍planned，transition回执数量0；慢SQL约5秒安全失败。
- 原网页回执修复 `.87` 已推送 Draft PR #280，远端run34744637014仍在执行。
- `.84` run34743886396完整CI已通过，不将其绿色结果算给本候选。

逐接口台账新增11个已读源码并有证据的研究GET，合计87/556。其余469接口及9个人工入口面继续审计。
列表200/64条仅目录展示，不作原提交不存在的依据；原键核对使用精确查询。网页工具F7纠错仍未关闭。
未部署、未调用真实模型、不推进Product Phase。
