# H4 第一切片：HTTP 研究任务提交凭据

2026-09-13，本地维护；Product Phase 97 不变。

研究创建入口直接使用 crypto.randomUUID，在普通 HTTP 环境中可能在持久化提交凭据之前抛错。
现复用既有 createRequestId，保留写前 localStorage 保存、原请求重用、归属隔离和精确回执清除。
不扩展领域权限，不改变 Backend 幂等合同，不宣称解决跨标签页竞争或所有未知提交。

定向 Vitest 7项通过，前端类型检查通过。新增回归模拟非安全上下文缺少 randomUUID，
验证生成一次、原凭据持久回读、重试复用、无关回执不能清除、精确回执清除。
这是组件证据；本批未运行真实 Product API 浏览器，须由后续受影响远端 CI 补齐。
未调用模型、未变更生产。H4 全接口原键核对、有界纠错及 H5 仍未完成。

最终构建为 .57；.56 保留为选择器同步前的历史清单。最终轻检查、隔离工作树和清单校验通过。
