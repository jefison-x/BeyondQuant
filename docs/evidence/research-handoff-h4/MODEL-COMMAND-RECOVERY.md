# H4 模型绑定和档案删除原命令恢复

2026-09-13，候选 `.83`。

新增 owner/operation/resource/expected_version 唯一的领域回执，与绑定 CAS、档案软删除及其
解绑副作用同事务。保存 actor、目标 profile、原提交版本和解绑版本，不包含凭据内容。
回执写入失败必须回滚业务变更；直接删除回执也使用本次 RETURNING 行，避免后续版本覆盖本次结果。

Product 页面写前按用户/工作区保存原命令。刷新只读核对原版本及目标，不将当前绑定或 deleted
状态当作某次提交证据。版本冲突不自动换版本重试；旧数据没有原回执时保持 not_found。
无新 DSH/MCP 能力，无模型调用，无生产数据变更。按 ADR-0019 补领域审计，不改变模型解析边界。

验证：

- 新只读入口修复前 404 失败；命令/凭据/绑定/等待上限定向 15 项通过。
- 追加绑定及删除回执故障两类回滚断言，命令文件 3 项通过：原绑定、档案版本和回执数量均未变化。
- 前端模型命令、档案、凭据及 settings 4 文件 12 项通过，覆盖错资源/目标/版本及持久存储失败。
- 前端构建通过；真实 Gateway/Product API 浏览器既有凭据/档案两项通过。
  新绑定→丢应答→刷新核对→删除→丢应答→刷新核对通过，两个 POST/PUT 各只有一次，解绑版本只增加一次。
- 初始浏览器定位混淆 input/listbox，随后点击外层节点未能展开；按真实控件 wrapper 滚动、点击并
  断言展开后完整通过，再连续复验两次通过；没有强制点击、扩大测试超时或改写产品返回。

台账现有 62 条具名验证，源枚举 554 条，仍有 492 条待最终映射，另九类手工 surface 未闭合。
`.80` 远端完整 CI run 34741512337 通过，不能覆盖之后 `.82/.83` 的新源；新源须重新执行远端 CI。
H4 全接口/纠错与 H5 完整研究仍继续。
