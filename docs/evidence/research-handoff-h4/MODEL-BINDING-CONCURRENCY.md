# H4 模型绑定并发与逐接口台账起稿

2026-09-13，`.78`。

失败测试用两个独立数据库连接把版本读取停在同一提交之前，证实相同expected_version的两次
模型绑定写入均成功。修复将version条件放入数据库冲突更新，等待上限2秒；只有一份原版本
可推进，另一份明确409。返回本次原子写入得到的版本，不用之后的目录状态冒充此次结果。
没有更改模型目录、凭据解析权限或Provider，不调用模型。

- 修复前并发断言失败；修复后credential/user/policy/logout定向20项通过。
- 追加首次绑定（原版本0）和已有绑定两类并发，2项均通过。
- `.77`远端真实集成通过，frontend旧模拟账户mock使用无效account_id且用户缺workspace，删除流程未能完成。
  修订测试身份为当前合同值；完整mock浏览器20通过、10明确跳过（真实栈用例需专用环境），不扩大超时或放宽实际身份校验。

[INTERFACE-REVIEW.json](INTERFACE-REVIEW.json)逐项记录了16条Runtime控制面和21条用户/认证接口，
包含来源、owner、调用类型、授权、幂等、超时、错误、重试、权威状态、恢复、消费者与证据。
当前枚举548项，该37项记录不是全量审计通过。凭据/模型设置其余写入口、个人策略及其他接口族
仍须逐项核对，H4/H5不关闭。源文件摘要用于发现台账与代码漂移，不能替代人工审计。
