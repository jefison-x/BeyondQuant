# H4 个人审批策略原提交恢复

2026-09-13，候选 `.84`。

五类写入（偏好保存、规则创建/修改/删除、应用预设）公开入口必须提供写前保存的 request_id。
规范输入与原操作/资源一起匹配，owner 事务锁串行化规则与预设替换，业务效果、审计及原结果
同事务持久化。回执失败回滚，原键重试先找原结果，不能重新创建已删除规则、重复应用预设
或覆盖后续偏好。缺旧回执不从当前状态补造。内部独立调用可生成新命令，但不是公开请求重试路径。

页面按用户/工作区冻结原命令；刷新只读核对，显式重试沿用原键。预设/删除原有确认保留；
个人自动批准仍不能绕过平台审批。优先级页面上限同步后端1000，无效非有限数值在写前拒绝。

- 原请求合同用例最初422失败；修复后后端12项通过，含8连接只建一条规则/审计、五类回执故障回滚、
  删除后原创建/更新恢复、旧预设不覆盖新增规则、旧设置不覆盖后续修改及跨用户拒绝。
- 另补锁等待与缺请求身份：1项通过，2秒数据库锁上限内安全失败，释放锁后无迟到写入。
- 前端2文件9项通过；Gateway原键转发1项通过；前端最终构建通过。
- 真实Gateway/Product API浏览器五类命令连续丢应答、刷新、GET核对通过，共5次写请求/5个不同命令键，
  没有重复创建或覆盖。最初停在不可见原生switch input，改为实际可见开关外框操作后通过；最终构建复验通过。
- 原有真实Product浏览器夹具同步必填请求键，未给旧版本添加兼容分支。

逐接口台账76项具名验证，源枚举556项，480项仍待最终映射，九类手工surface仍未闭合。
`.83`远端完整CI run 34742711828通过，不覆盖本候选新源。H4全接口/纠错与H5完整研究继续；
未调用付费模型、未合并或部署。
