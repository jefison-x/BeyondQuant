# H4 模拟账户并发与原订单恢复

2026-09-13，`.74`，Accepted ADR-0021业务不变量修复；仅模拟交易，不接实盘。

失败测试已证明：换绑后原键重放因版本推进被拒；1500元模拟账户的两个独立连接同时提交
各1000元买单时，两笔均filled，违反现金约束。不是仅根据代码推断，也未声称生产已发生。

订单、结算、风险控制、换绑和删除在同一账户行锁内校验并写入，锁等待有2秒上限。
新订单/换绑对目标池加共享锁，防止校验与提交之间目标池生命周期变化。
原订单按原键先读取持久冻结快照身份，再校验原输入哈希，池停用或后续快照变化不破坏旧回执。
重复订单不再次写成交、流水或领域引用。换绑先核对原键，再判断新写是否满足版本条件。

- 修复前2项明确失败；修复后恢复、paper_trading、paper_api共18项通过。
- 追加同键并发一次成交、一次现金变化，股票池停用后原订单回放、改价仍拒绝，恢复4项通过。
- 两个竞争不同标的订单仅一笔filled，另一笔blocked，最终现金等于append-only ledger现金合计。
- 没有生产、模型或外部Provider调用，无UI变化。

该切片不关闭账户创建、Product页面原请求保存、其他模拟命令只读回执及全H4/H5。
