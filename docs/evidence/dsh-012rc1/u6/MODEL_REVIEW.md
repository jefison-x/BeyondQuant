# U6 actual-model evidence interpretation

Status: IN_PROGRESS. Core rollback/Chrome checks passed; G2 requires a targeted
object-context qualification before T31 or release readiness can pass.

Core scope `byq-u5-u6-mr5ccwtq` used the retained exact CI images and completed
old→new→old, logical backup/actual restore, queued G6 approval continuation and
both G5 follow-ups. Its raw automated result is PASS, cleanup PASS. This does not
override the semantic G2 finding below.

| Fixed scenario | Observed current-build result |
|---|---|
| G1 | 10.480 s; no writes. Chrome inspection confirmed correct natural date and explicit unavailable-calendar limitation. |
| G2 without object locator | Automated answer/count checks passed in 185.627 s, but semantic review rejected qualification: it requested the backtest ID rather than analyzing the completed result. |
| G3 | 265.304 s; one approval, two research artifacts, zero training/prediction runs. One bounded continuation retry, no duplicate approval or object group. |
| G4 | 68.886 s; exactly one persisted Web Research Evidence artifact with URL-bearing sources and the required research-only policy. |
| G5 | Candidate and rollback follow-ups used the same public conversation and preserved domain counts. |
| G6 | One pending approval across maintenance, one feedback, one fake-Hub attempt/intake, zero publications, including duplicate continuation. |

The synthetic completed job existed: `backtest_1418b704b2f8488ca531e3a323d2386a`,
name `U5 TEST completed small backtest`, Product state `completed`. The original
probe sent only the fixed G2 text, without an object locator. The current summary
tool requires a precise job ID; the answer incorrectly tried the research task
identity, reported a 404, and asked for the backtest ID. No financial result was
invented and no object was mutated, but this is not successful backtest analysis.
The original raw automated result and this contrary semantic finding are retained.

The correction adds only the maintainer-authorized BYQ synthetic object context,
validated through the same owner's Product API. The fixed G2 prompt bytes are
unchanged; application code, tool authority and U6.3 images are unchanged. The
new assertion requires a successful HTTP 200 summary read for that exact job,
not merely an answer or a failed attempted read. Negative tests reject missing,
inaccessible/non-synthetic/running objects, a wrong job, 404 and non-summary calls.
One targeted G2 run is authorized by the existing bounded synthetic evaluation
scope; no unchanged scenario is rerun to improve a reported success rate.
