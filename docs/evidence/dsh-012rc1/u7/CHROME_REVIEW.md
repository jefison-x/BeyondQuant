# U7 exact-artifact Chrome review

Status: maintenance, candidate completion and Plugin Center review PASS.
2026-09-07 Asia/Shanghai. Chrome MCP page 6, isolated context
`byq-u7-final-build-review`; loopback test frontend `http://127.0.0.1:18210`.
Synthetic scope `byq-u5-u6-0tth2mjd`, exact retained U7.3 CI images.
No Cloudflare tab or production feedback record was operated.

Conversation `conversation_ec88635e01534255b569797e69fcda0c` generated one pending
approval `agent_approval_ab0ef9854d534fb4b1767ded526c97c8`, for synthetic feedback
`feedback_ddddd152045e4c4cba1b46a354706741`. The global approval drawer displayed
that same item. Chrome did not click approve/reject; the bounded runner owns
the synthetic continuation sequence.

Two actual Send clicks with fixed G5 during closed admission returned 503
(network request IDs 45 and 47). At both 1440×1000 and 390×844, actual DOM checks
confirmed: input equals the full fixed prompt; maintenance/input-retained message
visible; rejected G5 absent from accepted transcript; no processing indicator;
document width equals viewport. The pending approval remains durable and visible.

All 17 observed application requests used the same loopback Gateway origin and
auth/Product/Agent/normalized WorkflowTrace routes. Initial anonymous auth 401 is
expected; login/history/approval/trace requests succeeded. No direct Backend/MCP/
DSH/database or real Hub browser request. UI's generic central-Hub wording alone
does not prove isolation; actual immutable container/network and fake-Hub counters
are independently checked by the live runner. No screenshot is claimed.

## Promoted completion and plugin identity

After the runner's queued approval continuation and contextual G5, reload of the
same mobile conversation displayed the completed submission and contextual public
follow-up. The accessibility snapshot reports the approval button as no pending
work; there is no processing indicator, and the document remains 390 pixels wide.
The initial historical pending preview is retained alongside its later approved
result rather than rewritten. The two rejected browser sends were not accepted
turns; only the runner's later successful G5 appears in the persisted transcript.

Plugin Center at 390×844 and 1440×1000 displayed actual SDK/runtime-bin 0.1.2rc1,
profile `byq-product-candidate`, composition
`sha256:e584cee23c7e39ffaaf7d4c583d4762556032b4d4d173285c14c038d98ec6f98`,
Desired/Active matched, three qualified/active and two blocked plugins. Ask-user
and Spill remained blocked. Desktop web-search detail reports the actual carrier
package `deepseek-harness-runtime-bin@0.1.2rc1`, qualified and compatible. The U6
carrier qualification reference is correctly separate from this U7 final-image
review. No governance request, assignment change or deployment action was made.

Plugin-page network review: all seven observed application requests were
same-origin GETs and succeeded, including Product plugins and bounded sessions.
The detail drawer was inspected read-only. Both document widths matched viewport.
This review verifies the actual retained-image Product flow; production smoke and
the separate U8 observation remain NOT_RUN.
