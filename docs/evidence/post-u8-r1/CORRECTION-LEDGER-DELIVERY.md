# 当前整改分支同步与合并

2026-09-08，维护者明确要求：“先同步，合并代码。然后再继续整改。”
本次授权覆盖 `fix/post-u8-correction-ledger` 推送到 `jefison-x/BeyondQuant`、创建 PR，
通过当前远端 CI、贡献及 ADR-0015/0059 门禁后 squash auto-merge；不含生产部署、
release/tag、历史任务续跑或扩大数据目录。ADR-0066/0067 原“不授权 push/merge”描述
保留其当时事实；本条记录维护者随后单独给出的本分支交付授权，不扩大架构权限。

范围：根隔离与持久纠错准入、历史指数导入保护与明确日期、原会话任务发现，及对应
测试/失败证据。Product Phase 97 不变，U8 提前结束结论不变，DSH 保持 0.1.2rc1。
合并代码不是全部 Post-U8 验收完成；F6 累计预算执行资格、S3 历史准备与真实只读缓存、
F7 剩余迟到请求/取消竞争及完整研究语义仍按各自记录推进。后台主动续接保持关闭。

当前 source identity 为 `.12`，历史清单与失败不覆盖。已有当前 Backend 490 passed/
1 skipped/7 subtests、架构 224 和 MCP 全套通过，三根真实恢复链通过；同根双子任务
并发探针失败已解释为当前明确串行组合，未改记 PASS。远端 PR CI 状态以平台精确 head
查询为准，不将 `.10` 全量历史证据冒充当前版本全量验证。

同步检查：远端主线没有本地分支未包含的提交，工作树干净；未重写或强推历史。
PR 创建、远端 CI 和合并结果在完成后记录，不预填成功。
