# 源码公开准备与托管 CI 迁移证据

日期：2026-09-05。专项维护，不推进 Product Phase 或 DSH U0–U8。

## 授权与边界

- 维护者授权许可/权属/贡献/第三方边界落盘、合并 DSH 升级方案、公开源码和迁移标准免费 runner。
- 原创代码版权确认见 [OWNERSHIP](../../legal/OWNERSHIP.md)；联系入口为 GitHub 主页。
  维护者明确允许保留历史提交邮箱，不为此改写历史。
- 最新许可决定：禁止一切实盘用途，包括个人自有资金；不是绝对免责或 OSI 开源许可。
- [ADR-0060](../../architecture/adr/ADR-0060-source-publication-and-hosted-ci.md) 记录唯一发布准备
  PR 的一次性过渡例外；正常检查仍须成功，后续 runner PR 恢复严格服务器门禁。
- 不部署/重启业务服务，不更新 DSH runtime，不创建二进制发行物或 v1.0 标签。

## 发布准备本地验证

- 原治理完整隔离回归见 [治理证据](../governance-ci-hardening/README.md)。
- 发布准备架构/许可检查：99 tests PASS；含实盘禁止、第三方许可边界、贡献版权保留、DSH 仅规划，
  以及 8 项精确 head/协议摘要/作者/维护者审查的贡献授权检查。
- 四份 npm lock 生成确定性许可清单并检查；Python 清单仅为直接依赖声明，不冒充完整 SBOM。
- 隔离工作树验证、diff whitespace 检查通过。新增许可证与规范性文档始终进入架构检查。
- 待远端 PR 精确 head 的完整 CI；本地检查不是远端 CI，尚未消耗过渡合并例外。

## 公开前审计与平台迁移状态

前置只读审计已覆盖 223 个远程分支、240 个 PR 引用、582 个可达提交、3,633 个 blob，
以及 521 次当时可下载 CI 日志。24 条历史规则匹配及 1 条日志匹配经检查为测试/文档，
未确认真实机密泄露；本地已配置机密精确匹配为零。自动扫描不是不存在隐私/密钥的保证。

最终公开审计已完成：143 张历史截图本地 OCR 无机密规则/配置值命中；两张无文字截图经人工
复核，一张截断 PNG 仅可恢复页面顶部且经浏览器人工检查。GitHub Issue/PR、18 条普通评论、
2 条独立 review 共 4,338 行重扫无命中，未发现附件。最终完整历史仍为 24 条已逐项复核的
测试假值、幂等键或文档措辞误报；本次新增提交和最终 PR 日志均为零命中。扫描不能保证绝对安全。

唯一 Actions cache（约 32.7 MB、binfmt 构建缓存）已按精确 ID 删除，可由 GitHub 重建；未删除
源码、日志、提交或生产数据。PR #242 的精确 head、全绿检查、自审和一次性例外消耗已记录在
该 PR 审计评论，并正常 squash 合并。仓库已公开；Actions 切换期间暂停。正式机 runner 在 idle
状态停止并禁用，GitHub 注册已撤销，仓库 runner 数为零；生产容器和数据库未重启或修改。

`main` 已配置 strict/up-to-date 的 `local-ci` + `ci-gate`、管理员受约束、PR/线性历史/对话解决、
禁止 force push/delete。默认 Actions token 为 read，不能批准 PR；所有外部贡献者 workflow 需
维护者批准。hosted-only PR 仍须实际通过标准 `ubuntu-24.04` 完整 CI 后才算迁移完成；不复用
一次性例外，也不把瞬时 Git SHA/PR 状态写入 STATUS。
