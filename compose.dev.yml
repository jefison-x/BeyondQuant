# Local debugging override only. This is not a production deployment config.
services:
  gateway:
    ports:
      - "127.0.0.1:8100:8100"
  mcp:
    ports:
      - "127.0.0.1:8300:8300"
  backend:
    ports:
      - "127.0.0.1:8000:8000"
  runtime-adapter:
    ports:
      - "127.0.0.1:8400:8400"
